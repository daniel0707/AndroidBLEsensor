<?xml version="1.0" encoding="utf-8"?>

<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
			android:orientation="horizontal"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:columnCount="2">

	<TextView android:text="@string/deviceId_label"/>

	<TextView android:id="@+id/deviceId"/>

	<TextView android:text="@string/temp_label"/>

	<TextView android:id="@+id/temperature"/>

	<TextView android:text="@string/humidity_label"/>

	<TextView android:id="@+id/humidity"/>

	<TextView android:text="@string/luxometer_label"/>

	<TextView android:id="@+id/luxometer"/>

</GridLayout>